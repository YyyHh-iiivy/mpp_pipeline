# Week 2 板端验证记录

更新时间：2026-06-08

## 2026-06-08 - 开发机构建验证

构建命令：

```bash
cd /home/ubuntu/.config/superpowers/worktrees/workspace/week2-mpp-pipeline/big_core
RTT_EXEC_PATH="/home/ubuntu/k230_sdk/toolchain/riscv64-linux-musleabi_for_x86_64-pc-linux-gnu/bin" scons -j4
```

预期：

- 生成 `big_app.elf`。
- 新增 `stream_export.c` 和 `osd_module.c` 被显式编译。
- 不出现 `k_venc_pack` 字段错误或未定义符号。

结果：

- PASS：开发机构建通过。
- 备注：链接器仍打印 `LOAD segment with RWX permissions`，这是修改前基线已有警告。

## YYYY-MM-DD HH:mm - 板端短测

构建命令：

```bash
cd /home/ubuntu/workspace/big_core
RTT_EXEC_PATH="/home/ubuntu/k230_sdk/toolchain/riscv64-linux-musleabi_for_x86_64-pc-linux-gnu/bin" scons -j4
```

运行命令：

```bash
./big_app.elf
```

预期：

- NALU 持续输出。
- Ctrl+C 后 `pthread_join()` 能返回。
- cleanup 完整。

关键日志：

```text
[MPP] OSD init stub: waiting for VENC 2D OSD MMZ buffer wiring
[MPP] Stream export mode: local-log, timeout=200ms, max_packs=8
[MPP] Get NALU, Size: ...
[MPP] === Stats: ...
[MPP] Stream thread exit. Total: ...
[MPP] Cleanup complete
[MPP] Pipeline test PASSED.
```

结果：

- PASS 或 FAIL：
- 平均 fps：
- 是否有异常日志：
- 下一步问题：

## YYYY-MM-DD HH:mm - 10 分钟稳定性测试

构建命令：

```bash
cd /home/ubuntu/workspace/big_core
RTT_EXEC_PATH="/home/ubuntu/k230_sdk/toolchain/riscv64-linux-musleabi_for_x86_64-pc-linux-gnu/bin" scons -j4
```

运行命令：

```bash
./big_app.elf
```

预期：

- NALU 持续输出。
- 600 秒不死机。
- 自动退出后 cleanup 完整。
- 无 malloc 失败日志。

关键日志：

```text
[MPP] Auto-exit after 600 seconds.
[MPP] Stream thread exit. Total: ...
[MPP] Cleanup complete
[MPP] Pipeline test PASSED.
```

结果：

- PASS 或 FAIL：
- 平均 fps：
- 是否有异常日志：
- 下一步问题：
