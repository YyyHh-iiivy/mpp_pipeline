# 程序流程

本文用于同步记录当前 `big_core/mpp_pipeline` 的代码结构、调用关系和数据流。每次新增或修改代码文件后都应同步更新。

调试问题、证据、实验结论、当前稳定基线和上板验收模板统一记录在根目录 `debug日记.md`；本文只维护当前代码架构、调用链和数据流。

文件命名前缀按任务目标划分：

- `media_*`：大核 MPP 底层媒体能力，包括 VB、VICAP、VENC、硬件绑定、OSD 和资源清理。
- `stream_*`：H.265/NALU 码流出口和小核 RTSP/RTP 对接准备。
- `small_core/` 是从外部工程迁入后的唯一维护来源，包含 `small_diag`、DATAFIFO、RTP、RTSP、快照源码及测试；`small_core/user/` 中选定的可执行文件也纳入 Git，供 GitHub 直接下载。
- `motion_*`：AI 低清帧、运动检测算法契约和运动事件闭环。
- `mpp_pipeline.*`、`mpp_types.h`、`mpp_nalu_ipc.h` 保持为总入口和公共契约。
- `big_core/SConscript` 是当前应用实际生效的顶层源码白名单；新增的 `stream_freshness.c` 同时登记在顶层和模块内 `SConscript`，不使用 glob。

## 1. 函数抽象

### big_core/mpp_pipeline/mpp_types.h

- `AI_GRAY_MAX_WIDTH / AI_GRAY_MAX_HEIGHT -> compile_time_contract`
  输入：无运行期输入。
  输出：AI 灰度检测输入的最大宽高，当前为 `640x480`；`mpp_pipeline.h` 的 `AI_WIDTH`、`AI_HEIGHT` 与其保持一致。

- `ai_gray_frame_view(frame_id, timestamp_ms, width, height, stride, y) -> ai_luma_view`
  输入：AI 低清通道 dump 出来的 NV12 Y 平面元数据。
  输出：运动检测只读灰度视图；`y` 指向当前进程可读的 Y 平面，调用方不转移所有权，处理完仍由 `ai_frame_release()` 释放。`stride` 是相邻两行 Y 数据起点之间的字节跨度，可能因为 DMA 对齐大于 `width`，算法遍历第 `row` 行时必须使用 `y + row * stride`。

- `motion_detect_result(is_motion, motion_score) -> detection_result`
  输入：运动检测算法填充。
  输出：`is_motion != 0` 表示检测到运动；`motion_score` 为算法给出的强度分数。默认弱实现使用变化像素百分比，当前 B 侧强实现使用抽样变化点千分比，适配层只透传分数，不依赖具体量纲做阈值判断。

### big_core/mpp_pipeline/motion_detect.h

- `motion_detect_process(frame: const ai_gray_frame_view *, result: motion_detect_result *) -> k_s32`
  输入：AI 灰度帧和检测结果输出结构。
  输出：`0` 表示处理成功；当前由 `motion_detect.c` 提供 B 侧强符号实现，并覆盖 `motion_detect_default.c` 的弱符号示例。B 侧实现只负责检测并填写 `result`，不能直接操作 OSD、VICAP、VENC、RT-Thread 线程或释放帧资源。

### big_core/mpp_pipeline/mpp_pipeline.c

- `main(void) -> int`
  输入：无；读取编译期宏配置，如 `SENSOR_TYPE`、`ENC_WIDTH`、`ENC_HEIGHT`、`DST_FPS`、`VICAP_OUTPUT_FPS`、`OUTPUT_BUF_CNT=3`、默认启用的 `AI_BRANCH_ENABLE=1`、`VENC_OSD_ENABLE=1` 和 `STREAM_EXPORT_DATAFIFO`。两个开关仍可通过编译参数显式置零生成对照 ELF。
  输出：程序退出码；`0` 表示初始化、运行、清理完成，非 `0` 表示失败。任何 MPP 初始化前打印 `[diag] ai_branch=1`；启用 OSD 时同时打印 `[diag] experiment=ai_osd_restore venc_osd=1 runtime_buffer_writes=1`。默认构建会调用 `ai_motion_thread_start()`。

- `sig_handler(signo: int) -> void`
  输入：`SIGINT` 或 `SIGTERM` 等信号编号。
  输出：无；只设置 `g_stop_signal` 和 `g_running = 0`。默认 AI 线程和显式 AI-off 对照都会随主线程退出；码流线程由独立的 `g_stream_running` 控制，会继续 drain VENC，直到 cleanup 明确停止。该函数不打印日志，避免在信号上下文里调用 `printf` 导致 Ctrl+C 退出卡住。

### big_core/mpp_pipeline/media_vb.c

- `vb_init(void) -> k_s32`
  输入：无；默认使用 `FRAME_BUF_SIZE`、`STREAM_BUF_SIZE`、`AI_CHN_BUF_SIZE/AI_BUF_CNT` 及主链路块数宏；显式 `AI_BRANCH_ENABLE=0` 时才跳过 AI 宏。
  输出：`0` 表示 VB pool 初始化成功；非 `0` 表示 SDK 调用失败。pool数量按AI/OSD组合为2、3或4：主帧和VENC码流pool始终存在，AI启用时增加低清帧pool，OSD启用时再增加一个 `OSD_BUF_CNT=1`、`OSD_BUF_SIZE=192KiB` 的独立pool。低延迟 RTSP 档保持 `INPUT_BUF_CNT=3`、`OUTPUT_BUF_CNT=3`，OSD用户态申请不再消耗VENC stream块。

### big_core/mpp_pipeline/media_venc.c

- `venc_create_chn(chn: k_u32, bitrate: k_u32) -> k_s32`
  输入：VENC 通道号、目标码率 kbps。
  输出：`0` 表示 H.265 CBR 编码通道创建成功，并把 `g_status` 推进到 `STATUS_VENC_CREATED`；非 `0` 表示创建失败。该函数按 `SRC_FPS=15`、`DST_FPS=15` 和 `ENC_BITRATE=1500kbps` 配置低延迟优先的 CBR 码控，与主通道 `VICAP_OUTPUT_FPS=15` 的请求输出节拍对齐；默认 `VENC_GOP=8`，自然关键帧间隔约 `533ms`，不启用运行期主动 IDR，也不启动通道。降低码率用于限制大面积运动时的关键帧突发，不改变分辨率、帧率或网络缓冲策略。

- `venc_start_chn(chn: k_u32) -> k_s32`
  输入：VENC 通道号。
  输出：`0` 表示 VENC 编码通道启动成功，并把 `g_status` 推进到 `STATUS_VENC_STARTED`；非 `0` 表示失败。

- `stream_thread(arg: void *) -> void`
  输入：RT-Thread 线程参数，转换为 VENC 通道号。
  输出：无；在 `g_stream_running` 为真时循环获取 VENC stream。每轮先调用 `stream_export_flush()` 独立推进小核 `READ_DONE` release callback，再用 `kd_mpi_venc_query_status()` 取得原始 `cur_packs`；查询数不超过 `VENC_MAX_PACKS=8` 时使用线程栈描述数组，超过 8 时按查询数量临时 `calloc` 完整描述数组，不再把 `output.pack_cnt` 静默截断为 8。`kd_mpi_venc_get_stream()` 返回零 pack 时不进入 freshness、统计或导出层；查询数或返回数超过 8 时，统计完整 stream 的字节和 PTS，输出限频 `[venc:pack-overflow]`，然后完整 `kd_mpi_venc_release_stream()` 并释放临时数组，不进入当前 8-pack DATAFIFO 协议。有效且不超限的 stream 继续经 `stream_freshness_observe()` 和导出层处理。`compact_diag_observe()` 在连续 `500ms` 无有效stream时输出 stall，停流期间每 `1s` 续报。每 `5000ms` 的 `[health:venc]` 除 fps/kbps、帧大小、PTS间隔、QP、pending 和丢帧外，还输出累计 `max_query_packs` 与 `pack_overflow_count`。

### big_core/mpp_pipeline/stream_freshness.h / stream_freshness.c

- `stream_freshness_init(tracker: stream_freshness_tracker *) -> void`
  输入：新鲜度状态对象。
  输出：无；清零基线、连续迟到计数、追帧状态、最大漂移和丢帧计数。

- `stream_freshness_observe(tracker, pts_us, now_ms, drift_ms, drop) -> k_s32`
  输入：非 Header 帧的微秒 PTS、当前单调时钟毫秒值和输出指针。
  输出：`0` 表示完成判断；PTS 为 `0` 时不建立基线。首个有效 PTS 建立 `(base_pts_us, base_now_ms)`，后续计算 `source_drift_ms=(now-base_now)-(pts-base_pts)/1000`。连续 `2` 帧超过 `70ms` 才进入追帧并令 `drop=K_TRUE`；追帧期间漂移降至 `35ms` 即恢复。PTS 或单调时钟回退时安全重建基线。

### big_core/mpp_pipeline/compact_diag.c / compact_diag.h

- `compact_diag_tracker_init(tracker, now_ms) -> void`
  输入：停流诊断状态和stream线程启动时的单调时间。
  输出：以当前时刻建立最后有效stream基线。

- `compact_diag_observe(tracker, now_ms, has_valid_stream, elapsed_ms) -> compact_diag_action`
  输入：当前时刻及本轮是否获得有效stream。
  输出：正常静默；无有效stream达到500ms时返回`STALL_START`，停流期间每1000ms返回`STALL_ONGOING`，有效stream恢复时返回`STALL_RECOVERED`及完整停顿时长。状态机不访问MPP硬件，仅决定日志时机。

- `stream_thread_stop(void) -> void`
  输入：无；由 `pipeline_cleanup()` 在停止 VICAP/解绑/反初始化 VICAP 后调用。
  输出：无；设置 `g_stream_running = 0`，有限等待 `g_stream_exit_sem`，成功后删除 stream 退出信号量并清空 `g_stream_tid`。

### big_core/mpp_pipeline/media_vicap.c

- `vicap_try_config(sensor_type: k_vicap_sensor_type) -> k_s32`
  输入：传感器类型。
  输出：`0` 表示 sensor 查询、device 配置和 VICAP 初始化成功。默认 `AI_BRANCH_ENABLE=1` 同时配置主通道 `VICAP_CHN_ID_0` 与 `VICAP_CHN_ID_2` 的 `640x480` 低清通道；显式设为0时只保留主通道。已启用的通道都通过 `vicap_set_channel_attr()` 下发 `VICAP_OUTPUT_FPS=15`。

- `vicap_config(sensor_type: k_vicap_sensor_type) -> k_s32`
  输入：首选传感器类型。
  输出：`0` 表示首选或 fallback 配置成功；非 `0` 表示全部失败。

- `vicap_start(void) -> k_s32`
  输入：无。
  输出：`0` 表示 VICAP stream 启动成功。

### big_core/mpp_pipeline/media_bind.c

- `vi_bind_venc(void) -> k_s32`
  输入：无；使用固定 `VICAP_CHN` 和 `VENC_CHN`。
  输出：`0` 表示主通道 `VI -> VENC` 硬件绑定成功。

### big_core/mpp_pipeline/stream_export.c

- `stream_export_init(mode: stream_export_mode) -> k_s32`
  输入：导出模式，当前常用 `STREAM_EXPORT_DATAFIFO` 或 `STREAM_EXPORT_LOCAL_LOG`。
  输出：`0` 表示导出层初始化成功；DATAFIFO 模式会同时初始化 `nalu_ipc_module`。初始化时清空快照请求 FIFO；仅 `AI_BRANCH_ENABLE=1` 时后续 AI 线程会使用该快照出口。

- `stream_export_request_snapshot(request: const snapshot_request_msg *) -> k_s32`
  输入：运动事件生成的快照请求，包含 `event_id`、`frame_time_ms`、`source_chn` 和路径提示。
  输出：`0` 表示请求已进入最多 `4` 个元素的快照 FIFO；非 `0` 表示队列已满，本次请求被丢弃。该函数不请求主动 IDR、不复制码流、不写 SD 卡；入队请求由后续码流处理等待编码器按 GOP 自然产生的 `K_VENC_HEADER`。

- `stream_export_submit_venc_stream(chn: k_u32, stream: const k_venc_stream *, release_by_caller: k_bool *) -> k_s32`
  输入：VENC 通道号、`kd_mpi_venc_get_stream()` 得到的 stream、释放责任输出指针。
  输出：`0` 表示导出层处理成功；本地日志模式限频打印 NALU 并设置 `release_by_caller = K_TRUE`，DATAFIFO 成功接管时设置 `release_by_caller = K_FALSE`，DATAFIFO 投递失败时返回错误并要求调用方立即释放。若存在待快照请求，会优先等待带 `K_VENC_HEADER` 的 stream，超过 `VENC_GOP + 2` 帧后兜底给后续帧设置 `MPP_NALU_IPC_FLAG_SNAPSHOT`。

- `stream_export_flush(void) -> k_s32`
  输入：无。
  输出：DATAFIFO 模式下触发 `nalu_ipc_flush()` 处理小核 `READ_DONE` 回调；本地日志模式直接返回 `0`。

- `stream_export_get_pending_count(void) -> k_u32`
  输入：无。
  输出：DATAFIFO 模式下返回 `nalu_ipc_get_pending_count()`；本地日志模式或导出层未初始化时返回 `0`。该函数只暴露 pending 深度给统计日志，不让 `media_venc.c` 直接依赖 DATAFIFO 细节。

- `stream_export_get_last_seq(void) -> k_u64`
  输入：无。
  输出：DATAFIFO模式返回最近成功提交的跨核seq，本地日志模式返回帧计数；供VENC停流摘要关联大小核最后一帧，不改变stream所有权。

- `stream_export_deinit(void) -> void`
  输入：无。
  输出：无；DATAFIFO 模式下会反初始化 `nalu_ipc_module`，并清空尚未投递的快照请求。

### big_core/mpp_pipeline/stream_nalu_ipc.c

- `nalu_ipc_init(void) -> k_s32`
  输入：无；使用 DATAFIFO writer 参数 `entries=3`、`NALU_IPC_PENDING_MAX=2`、`item_size=512`。
  输出：`0` 表示 DATAFIFO writer 打开成功，并打印给小核使用的 `phy_addr` 与 `pending_max/fifo_entries/output_buf/drain_max_passes` 配置。初始化时清零 pending 表、高水位、提交序号、drop/full/write 统计、最新提交 seq gap 和最近一次 `READ_DONE` 耗时记录。

- `nalu_ipc_submit_stream(chn: k_u32, stream: const k_venc_stream *, flags: k_u32) -> k_s32`
  输入：VENC 通道号、`kd_mpi_venc_get_stream()` 得到的 stream，以及写入 `mpp_nalu_ipc_msg.reserved` 的标志位。
  输出：`0` 表示已把 NALU 描述符写入 DATAFIFO；非 `0` 表示未投递成功，导出层会要求 `media_venc.c` 立即 release 当前 stream。函数每次尝试都会先分配递增 `seq` 和 `submit_time_ms`，再通过 `nalu_ipc_drain_releases()` 有界处理小核 `READ_DONE` 回调。若 DATAFIFO 可写空间不足则丢当前帧；若两个 pending slot 已满，会输出 slot 年龄、再 drain 并重试一次，仍无空位才记录 `pending_full_after_drain`。每次 pending 增长同时更新 `pending_high_water`。

- `nalu_ipc_drain_releases(void) -> k_s32`
  输入：无。
  输出：DATAFIFO `write(NULL)` 的结果；最多执行 `NALU_IPC_DRAIN_MAX_PASSES=4` 次，用于主动拉取 reader 侧 `READ_DONE` 通知并触发 release callback。若某次调用后 `g_pending_count` 没有下降，则停止本轮 drain，避免在异常状态下空转。

- `nalu_ipc_log_pending_slots(reason: const char *, avail_write_len: k_u32) -> void`
  输入：诊断原因和当前 DATAFIFO 可写长度。
  输出：无；在 pending 满等异常路径打印每个 pending 凭据的 `seq`、`chn`、`age_ms`、`total_len` 和 `avail`，用于区分“小核没有 READ_DONE”和“大核没有及时处理 release callback”。

- `nalu_ipc_flush(void) -> k_s32`
  输入：无。
  输出：`nalu_ipc_drain_releases()` 的结果；用于处理小核 `READ_DONE` 后触发的释放回调。

- `nalu_ipc_release_callback(datafifo_item: void *) -> void`
  输入：DATAFIFO release callback 传回的 `mpp_nalu_ipc_msg` 描述符指针。
  输出：无；用 `seq + chn` 找回 pending 表中的延迟释放凭据，计算 `nalu_ipc_now_ms() - submit_time_ms` 得到 `read_done_age_ms`，再重建 `k_venc_stream` 调用 `kd_mpi_venc_release_stream()`。callback 会保存最近一次 `read_done_seq/read_done_age_ms`；正常完成静默，只有耗时达到 `500ms` 才单独输出慢 `READ_DONE`。

- `nalu_ipc_get_pending_count(void) -> k_u32`
  输入：无。
  输出：当前已交给 DATAFIFO、但尚未收到小核 `READ_DONE` 的 stream 数量，目标稳定在 `0-2`，短时最多 `3`。

- `nalu_ipc_get_phy_addr(void) -> k_u64`
  输入：无。
  输出：DATAFIFO ring buffer 物理地址，供小核 `kd_datafifo_open_by_addr()` 使用。

- `nalu_ipc_deinit(void) -> void`
  输入：无。
  输出：无；flush DATAFIFO，强制释放未被小核 `READ_DONE` 的 pending stream，并关闭 DATAFIFO。

### big_core/mpp_pipeline/motion_ai_frame.c

- `ai_frame_channel_init(void) -> k_s32`
  输入：无。
  输出：`0` 表示 AI 帧读取逻辑初始化完成。

- `ai_frame_try_get(view: ai_gray_frame_view *, ai_frame_handle_out: void **) -> k_s32`
  输入：输出视图和 AI 帧资源句柄输出指针。
  输出：`0` 表示从 AI VICAP 通道 dump 到一帧并 mmap Y 平面；`view` 只描述 NV12 的 Y 平面，`ai_frame_handle_out` 返回 dump frame 和 mmap 生命周期凭证，调用者必须最终传给 `ai_frame_release()`。

- `ai_frame_release(ai_frame_handle_ptr: void *) -> k_s32`
  输入：`ai_frame_try_get()` 返回的 AI 帧资源句柄。
  输出：`0` 表示 munmap 并释放 dump frame 成功。

- `ai_frame_channel_deinit(void) -> void`
  输入：无。
  输出：无；输出反初始化日志。

### big_core/mpp_pipeline/motion_adapter.c

- `motion_adapter_init(void) -> k_s32`
  输入：无。
  输出：`0` 表示运动适配层初始化完成，并重置事件序号、连续运动帧计数、连续静止帧计数、锁存状态和上次事件时间。

- `motion_adapter_process(frame: const ai_gray_frame_view *, event: motion_event_msg *, has_event: k_bool *) -> k_s32`
  输入：AI 灰度帧、事件输出、是否有事件输出标志。
  输出：`0` 表示处理成功；内部调用 `motion_detect_process()`，先对算法输出做消抖，再把确认后的运动转换成 `motion_event_msg`。消抖规则为连续 `3` 帧运动才触发、连续 `5` 帧静止才解除锁存、同一段持续运动只发送一次事件、两次事件至少间隔 `1000ms`。确认后的事件会设置 `request_snapshot = 1` 和 `osd_duration_ms = 1000`，让 AI 线程同时触发 OSD 和快照申请；该函数不取帧、不释放帧、不直接控制 OSD。

- `motion_adapter_deinit(void) -> void`
  输入：无。
  输出：无；输出运动适配层反初始化日志。

### big_core/mpp_pipeline/motion_detect.c

- `motion_detect_validate_frame(frame: const ai_gray_frame_view *) -> k_s32`
  输入：AI 灰度帧视图。
  输出：`0` 表示 `frame`、`y`、宽高、`stride`、最大尺寸和 `width * height` 乘法范围都满足 B 侧算法要求；非 `0` 表示非法输入。该函数为文件内静态辅助函数。

- `motion_detect_store_frame(frame: const ai_gray_frame_view *) -> void`
  输入：AI 灰度帧视图。
  输出：无；按 `stride` 逐行复制有效 `width` 字节到上一帧缓存，缓存按 `width * height` 紧密排列，不保存每行 padding。该函数为文件内静态辅助函数。

- `motion_detect_process(frame: const ai_gray_frame_view *, result: motion_detect_result *) -> k_s32`
  输入：AI 低清通道 NV12 的 Y 平面视图。
  输出：`0` 表示处理成功；当前生产构建使用该强符号实现覆盖默认弱符号。算法首帧或分辨率变化时只重建上一帧缓存，后续每隔 `4` 个像素采样一次，统计亮度差大于等于 `25` 的采样点比例，并用千分比写入 `motion_score`；达到 `15` 时置 `is_motion = 1`。

### big_core/mpp_pipeline/motion_detect_default.c

- `motion_validate_frame(frame: const ai_gray_frame_view *) -> k_s32`
  输入：AI 灰度帧视图。
  输出：`0` 表示 `frame`、`y`、宽高、`stride` 和最大尺寸都满足默认算法要求；非 `0` 表示非法输入。该函数为文件内静态辅助函数。

- `motion_copy_baseline(frame: const ai_gray_frame_view *) -> void`
  输入：AI 灰度帧视图。
  输出：无；按 `stride` 逐行复制有效 `width` 字节到内部上一帧基线，不复制每行 padding。该函数为文件内静态辅助函数。

- `motion_detect_process(frame: const ai_gray_frame_view *, result: motion_detect_result *) -> k_s32`
  输入：AI 低清通道 NV12 的 Y 平面视图，可直接按灰度图处理；访问每行必须使用 `frame->stride`，不能假设 `stride == width`。
  输出：`0` 表示处理成功；默认弱符号实现采用简单帧差，首帧只建立基线，后续统计亮度差超过阈值的像素百分比并写入 `motion_score`，达到阈值时置 `is_motion = 1`。该文件是算法兜底示例；当前构建同时编译 `motion_detect.c`，链接时由 B 侧强符号覆盖它。

### big_core/mpp_pipeline/motion_thread.c

- `ai_motion_thread(arg: void *) -> void`
  输入：RT-Thread 线程参数，当前未使用。
  输出：无线程返回值；循环执行 `ai_frame_try_get()` 和 `motion_adapter_process()`，随后先用局部变量 `ai_frame_handle` 调用 `ai_frame_release()` 释放 dump/mmap 资源，最后才按事件调用 `osd_set_motion_visible()` 和 `stream_export_request_snapshot()`。每个事件只输出一条 `[event:motion] id/score/duration_ms/osd_enabled/osd_ret/snapshot_ret`；默认 `osd_enabled=1`，快照请求仍独立执行。`ai_frame_handle` 不参与算法计算，只作为当前帧资源释放凭证。

- `ai_motion_thread_start(void) -> k_s32`
  输入：无。
  输出：`0` 表示 AI frame、motion adapter、RT-Thread 线程启动成功。

- `wait_ai_motion_thread_exit(timeout_ms: k_u32) -> k_bool`
  输入：等待 AI 运动线程退出的超时时间，当前由 `AI_THREAD_EXIT_TIMEOUT_MS` 给出。
  输出：`K_TRUE` 表示收到 `g_ai_motion_exit_sem`；`K_FALSE` 表示等待超时。该函数为文件内静态辅助函数。

- `ai_motion_thread_stop(void) -> void`
  输入：无。
  输出：无；停止 AI 线程，有限等待退出信号量；若线程仍未退出，则记录日志并继续后续硬件清理，避免 cleanup 永久阻塞。

### big_core/mpp_pipeline/media_osd.c

该文件以 `#if VENC_OSD_ENABLE` 保留完整硬件 OSD 实现，以 `#else` 提供 no-OSD 对照使用的无副作用桩。当前默认 `VENC_OSD_ENABLE=1`，因此最终 ELF 在 VENC 启动前 attach 2D 并配置固定 region，运行期只更新 OSD buffer；显式置零时才完全移除 VENC 2D、OSD VB/MMZ 和素材更新路径。

- `osd_init(void) -> k_s32`
  输入：无。
  输出：禁用态直接返回 `0`，不申请 VB/MMZ、不 attach 2D、不下发任何 2D 参数。启用态从独立OSD公共pool申请ARGB8888图片buffer并打印实际/预期pool ID，mmap cached后清零并flush cache、执行 `kd_mpi_venc_attach_2d()`，再以全局 `osd_alpha=255` 固定配置region 0；`kd_mpi_venc_set_2d_osd_param()` 仍只存在于该启用分支。

- `osd_update_buffer(visible: k_bool) -> k_s32`
  输入：目标显隐状态。
  输出：仅在 `VENC_OSD_ENABLE=1` 的启用分支编译；显示时把 512×96 ARGB 素材复制到约 192KiB 的 OSD 专用 buffer，隐藏时清零并 flush MMZ cache。禁用实验版没有该运行期函数和任何 buffer 写入路径。

- `osd_set_motion_visible(visible: k_u32, duration_ms: k_u32) -> k_s32`
  输入：显示标志和持续时间。
  输出：禁用态忽略两个参数并返回 `0`，不写任何状态。启用态才在短临界区内更新目标 visible、自动隐藏 deadline、请求代次和重试状态。

- `osd_poll_auto_hide(void) -> k_s32`
  输入：无。
  输出：禁用态直接返回 `0`，无轮询状态、无素材更新和日志。启用态才处理 deadline、调用 `osd_update_buffer()` 并输出受限频的 `[osd:buffer]`。

- `osd_deinit(void) -> void`
  输入：无。
  输出：禁用态为空函数；启用态才在 VENC stop 后 detach VENC 2D、munmap OSD 图片 buffer 并释放 VB block。

### big_core/mpp_pipeline/media_cleanup.c

- `pipeline_cleanup(void) -> void`
  输入：无；读取 `g_status` 判断资源初始化进度。
  输出：无；按退出依赖停止 AI、VICAP、stream 线程、导出层、VENC、OSD 和 VB。默认 OSD cleanup 在 VENC stop 后 detach 2D 并释放 buffer；显式关闭 OSD 时才落入空桩。

### big_core/mpp_pipeline/tests/test_motion_detect_b.c

- `main(void) -> int`
  输入：主机侧构造的灰度帧数组。
  输出：`0` 表示 B 侧帧差算法通过回归测试；覆盖首帧建基线、stride padding 不触发、整帧亮度变化得到 `1000` 千分比分数、`stride < width` 和超过 AI 最大宽度返回失败。

### big_core/mpp_pipeline/tests/test_motion_adapter_debounce.sh

- `test_motion_adapter_debounce.sh(root) -> shell_test`
  输入：仓库根目录，默认为当前目录。
  输出：临时编译 `motion_adapter.c` 和测试桩；测试桩提供可控的 `motion_detect_process()` 输出，验证连续 `3` 帧确认、持续运动锁存、连续静止释放、`1000ms` 冷却抑制立即重复触发，以及确认事件保持 `1000ms` OSD 显示时长。

### big_core/mpp_pipeline/tests/test_osd_buffer_rules.sh

- `test_osd_buffer_rules.sh(root) -> shell_test`
  输入：仓库根目录，默认为当前目录。
  输出：源码级锁定保留的 `VENC_OSD_ENABLE=1` 实现：VENC OSD region 只在 `osd_init()` 配置一次、运行期显示复制 ARGB 素材、隐藏清零 MMZ buffer、每次更新 flush cache，并验证 stream 线程调用顺序和 `[osd:buffer]` 字段，确保切回宏时原硬件实现仍完整。

### big_core/mpp_pipeline/tests/test_noosd_ab_rules.sh

- `test_noosd_ab_rules.sh(root) -> shell_test`
  输入：仓库根目录，默认为当前目录。
  输出：锁定 `VENC_OSD_ENABLE=0`、无 OSD 启动指纹、运动事件 `osd_enabled` 字段及独立快照请求；提取禁用分支，确认四个 OSD API 桩齐全且不含 VENC 2D、VB、mmap、cache flush、素材复制或 OSD 状态写入。

### big_core/mpp_pipeline/tests/test_compact_diag.c / test_compact_diag.sh

- `test_compact_diag -> host_test`
  输入：构造有效stream与无stream的单调时间序列。
  输出：验证500ms进入停流、每1000ms续报、有效stream恢复及新一轮停流重新计时；shell规则同时锁定60秒健康摘要、大小核诊断字段、SConscript白名单和正常高频日志禁用要求。

### big_core/mpp_pipeline/tests/test_frame_rate_config.sh

- `test_frame_rate_config.sh(root) -> shell_test`
  输入：仓库根目录，默认为当前目录。
  输出：检查 15fps 低延迟 RTSP 配置是否同时覆盖 `DST_FPS`、`VICAP_OUTPUT_FPS`、`VENC_GOP`、VICAP 通道属性和 VENC GOP 参数，避免后续误改回 30fps 或留下硬编码 GOP。

### small_core/lnc/small_diag.h / src/small_diag.c

- `small_diag_tracker_init(tracker, now_ms) -> void`
  输入：小核DATAFIFO发送线程启动时的单调时间。
  输出：清零停流状态，并把当前时刻设为最后有效DATAFIFO数据基线。

- `small_diag_observe(tracker, now_ms, has_valid_data, elapsed_ms) -> small_diag_action`
  输入：当前单调时间及本轮是否读取到通过协议校验且非回退的DATAFIFO消息。
  输出：正常静默；连续500ms无有效数据返回`STALL_START`，之后每1000ms返回`STALL_ONGOING`，有效数据恢复返回一次`STALL_RECOVERED`及完整停顿时长。

### small_core/lnc/rtp_clock.h / src/rtp_clock.c

- `rtp_clock_reset(clock_state, base_ts) -> void`
  输入：RTP 时钟状态和会话当前随机基准时间戳。
  输出：清空 PTS/rebase 状态并保留给定 RTP 基准；不修改 SSRC、RTP 序号或 RTSP 会话。

- `rtp_clock_next_timestamp(clock_state, frame_pts_us, pts_mode, pts_delta_us, rtp_delta) -> uint32_t`
  输入：上一帧时钟状态和当前微秒 PTS。
  输出：90kHz RTP 时间戳及诊断字段。有效 PTS 首次建立基线；正常前向增量不超过1秒时沿既有基线映射；前向增量超过1秒时按完整 PTS 差推进 RTP、令 `pts_mode=1` 并以当前 PTS/RTP 重建基线；PTS 回退或重复仍只推进 `RTP_TS_STEP` 后重建基线。两类 discontinuity 都增加 `rebase_count`，供发送线程进入自然随机访问门控。

### small_core/lnc/latency_profile.h / src/latency_profile.c

- `latency_profile_init(profile) -> void`
  输入：小核发送线程的延迟画像状态。
  输出：清零六类阶段统计和最近64个样本窗口，不影响码流处理状态。

- `latency_profile_observe(profile, msg_age_ms, copy_ms, read_done_ms, send_ms, outq_before, outq_after) -> void`
  输入：当前已完成发送帧的DATAFIFO消息年龄、复制、READ_DONE、RTP发送耗时及UDP队列前后值。
  输出：累计计数、平均值、P90和最大值；仅更新诊断内存，不改变发送、丢帧或恢复决策。

- `latency_profile_format(profile, buffer, buffer_size) -> int`
  输入：当前画像状态和输出缓冲区。
  输出：生成低频`count/msg_age/copy/read_done/send/outq`摘要；无样本返回0，供`RTSP_Create.c`每5秒异常画像窗口打印一次`[latency:profile]`后重置。

### small_core/src/RTSP_Create.c

- `small_diag_log_health(...) -> void`
  输入：诊断状态、read/read_done/sent seq、播放与随机访问等待状态、帧/丢包计数、UDP socket和实际发送缓冲。
  输出：从线程启动起每60秒输出一条`[health:small]`，关联DATAFIFO进度、空闲时长、RTP outq和`SO_SNDBUF`；不在启动首帧或每秒强制打印。

- `small_diag_log_stall(action, cause, elapsed_ms, ...) -> void`
  输入：`small_diag_observe()`动作、原因和当前DATAFIFO/播放状态。
  输出：只为start/ongoing/recovered动作输出`[stall:small]`，因此无数据500ms前静默，异常期最多每秒一条。

- `rtp_diag_log_frame(sock, diag, frame_seq, frame_rtp_ts, outq_before, query_failed, outq_after_out) -> void`
  输入：当前帧RTP时间戳及发送前后的UDP队列探针。
  输出：通过`outq_after_out`把发送后队列值交给延迟画像，同时更新队列高水位和RTP/墙钟漂移；正常帧静默，只在RTP step非正、队列查询失败或outq达到实际`SO_SNDBUF`的75%时输出`[rtp:diag]`。outq首次越过阈值立即打印，持续拥塞时每秒最多一条，降回阈值下后允许下次越界再次立即打印。

- `small_latency_profile_log(profile, last_log_ms) -> void`
  输入：小核发送线程延迟画像及上次摘要时间。
  输出：至少每5秒检查一次；有样本时输出一条`[latency:profile]`并清空当前窗口，没有样本时保持静默。

- `log_large_frame_stage(stage, seq, total_len, ret, cost_ms, threshold_ms) -> void`
  输入：大于等于64KiB帧的copy、READ_DONE或RTP阶段结果。
  输出：成功且copy/READ_DONE不超过5ms、RTP不超过15ms时静默；失败或超阈值才输出一条`[datafifo:large]`。

- `datafifo_rtp_sender_loop(sock) -> void *`
  输入：已配置非阻塞低延迟参数的UDP socket。
  输出：持续读取DATAFIFO；需发送或保存的pack先复制到小核自有内存，随即READ_DONE，再做RTP和异步快照。PTS重锁仍在处理当前帧前进入自然IDR等待；新增compact诊断只改变日志时机，不改变编码、DATAFIFO协议、RTP序号/SSRC和恢复策略。

### small_core/src/nalu_datafifo.c、datafifo_snapshot.c、snapshot_writer.c、rtsp.c

- `nalu_datafifo_read()/nalu_datafifo_read_done()`：空轮询和正常快照标志读/确认均静默；协议、mmap或READ_DONE失败仍立即打印。
- `send_rtp_packet()`：`EAGAIN/EWOULDBLOCK/ENOBUFS`仍立即丢当前包并触发自然随机访问恢复；首个busy立即打印，持续busy按1秒节流并累计drops，避免同一UDP拥塞按分片刷屏。
- `h265_snapshot_log_failure(stage, copied, frame, writer_ready, ret)`：同步快照失败只输出首个及每第10个`[snapshot:fail]`，单行保留stage、seq、flags、writer、IRAP/GOP和VPS/SPS/PPS状态，避免压缩日志后丢失“为何无法保存”的证据。
- `datafifo_snapshot_process_copied()`：持续维护VPS/SPS/PPS与GOP缓存；快照成功只保留简短captured信息并交给writer，writer未就绪、无法构造可播放码流或入队失败统一调用上述失败合并器，不改变`reserved bit0`语义。
- `snapshot_writer_enqueue_h265_take()`：成功入队静默，后台保存成功保留一条`.h265`路径日志；异步mkdir/open/write/close失败仍保留直接证据。
- `handle_rtsp_client()`：OPTIONS keepalive继续正常响应但不打印完整请求；其他RTSP方法只输出method/url/cseq/from单行摘要。

### small_core/tests/test_compact_diag.c / test_compact_diag.sh

- `test_compact_diag -> host_test + source_rules`
  输入：构造500ms/1s/恢复时间序列，并扫描小核生产源码、Makefile及日志字符串。
  输出：锁定60秒健康、500ms停流、1秒续报、慢路径阈值、OPTIONS静默、快照失败合并，以及独立`user/rtsp_sender_withsd_compactdiag`产物；`make verify`同时确认旧`rebase_idrwait`仍存在。

### small_core/tests/test_latency_profile.c / test_latency_profile.sh

- `test_latency_profile -> host_test + source_rules`
  输入：两组阶段耗时和UDP队列样本。
  输出：验证空画像静默、平均/P90/最大值计算、日志字段及Makefile集成。

## 2. 调用链

### 初始化链路

```text
main()
-> vb_init()
-> venc_create_chn(VENC_CHN, ENC_BITRATE)
   -> kd_mpi_venc_create_chn()
   -> g_status = STATUS_VENC_CREATED
-> osd_init()，默认 VENC_OSD_ENABLE=1 时配置硬件 OSD region
-> venc_start_chn(VENC_CHN)
-> vicap_config(SENSOR_TYPE)
-> vicap_try_config()
-> vi_bind_venc()
-> vicap_start()
-> stream_export_init(STREAM_EXPORT_DATAFIFO 或 STREAM_EXPORT_LOCAL_LOG)
-> 默认 AI_BRANCH_ENABLE=1 时 ai_motion_thread_start()；显式0时跳过
-> rt_thread_create(stream_thread)
```

关键数据：

- 默认 `[diag] ai_branch=1` 和 `experiment=ai_osd_restore` 用于确认恢复版 ELF；`vb_init()` 准备主帧、VENC 码流和 AI 低清三类内存池。
- `venc_create_chn()` 创建 VENC 通道并设置 `STATUS_VENC_CREATED`，但不启用主动 IDR，也不启动编码；CBR 输入/目标帧率为 `15/15fps`，默认目标码率为 `1500kbps`，自然 GOP 为 `8`，VENC stream buffer 数量为 `OUTPUT_BUF_CNT=3`。
- 当前 `VENC_OSD_ENABLE=1`，`osd_init()` 申请 OSD buffer、attach 2D 并在 VENC start 前下发一次固定 region 参数；运行期只复制/清零素材并 flush cache。显式置零时使用无副作用桩。
- `venc_start_chn()` 在禁用态直接启动原始编码通道，编码分辨率、码率、FPS 和 GOP 不变。
- 默认 `vicap_try_config()` 建立主通道 `1920x1080 NV12` 和 `VICAP_CHN_ID_2` 的 `640x480` 低清旁路，并请求 `VICAP_OUTPUT_FPS=15`；`AI_BRANCH_ENABLE=0` 时才跳过旁路。
- `vi_bind_venc()` 只绑定主通道到 VENC。
- `stream_export_init()` 决定后续导出后端：本地日志立即释放，DATAFIFO 等小核释放；默认 AI 运动事件会登记独立快照请求，DATAFIFO、码率和 RTP 参数保持不变。
- `g_stream_running` 在线程启动前置 `1`；`Ctrl+C` 只关闭 `g_running`，不立即关闭 stream 线程，保证退出清理阶段还能继续消费 VENC buffer。

### 本地日志码流链路

```text
stream_thread()
-> osd_poll_auto_hide()，禁用态直接返回0且不写buffer
-> stream_export_flush()
-> nalu_ipc_drain_releases()，独立推进 READ_DONE callback
-> kd_mpi_venc_query_status()
-> kd_mpi_venc_get_stream()
-> query/get失败或pack_cnt == 0: compact_diag累计无有效stream时长，500ms前静默，之后按1s输出stall并重试
-> venc_stream_video_pts()
-> 有效stream: compact_diag输出必要的recovered并重置停流计时
-> stream_freshness_observe()
   -> stale: kd_mpi_venc_release_stream()，继续追帧
   -> fresh: 继续导出
-> stream_export_submit_venc_stream()
-> release_by_caller = K_TRUE
-> kd_mpi_venc_release_stream()
```

关键数据：

- `k_venc_stream` 直接提交给导出层，本地日志后端只读取 pack 的 `len` 和 `type`。
- `stream_export_submit_venc_stream()` 在本地日志后端限频打印 `Get NALU, Size: ...`，配合 `stream_thread()` 每轮主动让出调度，避免串口/终端输出和高优先级采集线程拖慢 Ctrl+C 退出响应。
- 打印完成后立即释放 VENC stream。
- 本地日志模式 `stream_export_get_pending_count()` 始终返回 `0`。

### DATAFIFO 码流链路

```text
stream_thread()
-> osd_poll_auto_hide()，禁用态直接返回0且不写buffer
-> stream_export_flush()
-> nalu_ipc_drain_releases()，独立推进 READ_DONE callback
-> kd_mpi_venc_query_status()
-> cur_packs <= 8: 使用栈上 packs[]；cur_packs > 8: 按查询数量临时分配描述数组
-> kd_mpi_venc_get_stream()
-> pack_cnt == 0: 不进入导出层，5ms 后重试
-> query/returned pack_cnt > 8: 记录 pack-overflow，完整 release stream，释放临时数组后丢弃当前帧
-> stream_export_submit_venc_stream()
-> nalu_ipc_submit_stream(..., flags)
-> nalu_ipc_drain_releases()
-> kd_datafifo_write(mpp_nalu_ipc_msg)
-> 小核 mmap phys_addr 读取 NALU
-> 小核检查 reserved 快照标志并在 READ_DONE 前复制被标记帧
-> 小核 DATAFIFO_CMD_READ_DONE
-> 小核small_diag_observe()更新有效数据/停流状态
-> 小核发送前 rtp_get_send_queue_bytes(SIOCOUTQ)
-> 小核 H.265 RTP 分片与 sendto()
-> 小核发送后再次读取 SIOCOUTQ；仅队列达到SO_SNDBUF 75%、时间戳异常或查询失败时输出rtp:diag
-> stream_export_flush() 或 nalu_ipc_deinit()
-> nalu_ipc_release_callback()
-> kd_mpi_venc_release_stream()
```

关键数据：

- 大核写入 DATAFIFO 的不是 NALU 内容，而是 `mpp_nalu_ipc_msg` 描述符。
- 每条消息包含 `seq`、`frame_pts`、`submit_time_ms`、`total_len` 和 `reserved`；每个 pack 包含 `phys_addr`、`len`、`pts`、`type`。`reserved` 的 bit0 是 `MPP_NALU_IPC_FLAG_SNAPSHOT`，用于通知小核复制该帧码流并投递快照队列。
- 当前跨核协议保持 `MPP_NALU_IPC_MAX_PACKS=8`。VENC query/get 不再受该协议容量截断；超过 8 pack 的 SDK stream 在大核完整获取和释放后丢弃，避免不完整 release 耗尽 VENC output buffer。
- DATAFIFO ring entries 保持 `3`，pending 表容量为 `2`，VENC 输出 buffer 数量为 `3`；两个 slot 覆盖 `15fps × 100ms ≈ 1.5` 个实测在途帧，并保留 1 个 VENC buffer 余量。
- `media_venc.c` 不直接调用 `nalu_ipc_*`，只根据导出层返回的 `release_by_caller` 决定是否立即释放。
- 大核不能在小核 `READ_DONE` 前释放 stream。
- 若 DATAFIFO 无可写空间、pending 表满或写入失败，导出层返回错误并要求 `stream_thread()` 立即 release 当前 stream，形成“丢当前帧、保留已有 pending 生命周期”的安全低延迟策略。pending 满时大核会先有界 drain release 通知并重试一次；仍失败才丢当前帧。大核不会仅凭 `avail_write_len` 变大就强行释放仍可能被小核使用的旧 DATAFIFO 描述符。
- 小核 DATAFIFO 读端必须保证 `kd_datafifo_read()` 成功后最终调用 `DATAFIFO_CMD_READ_DONE`；RTSP 发送、网络阻塞、SD 卡写入、JPEG/快照保存不能阻塞在 `READ_DONE` 前。快照帧只允许在 `READ_DONE` 前复制必要编码数据，复制后应立即 `READ_DONE`，后续保存异步处理。
- 如果本次 stream 计划携带快照标志但 DATAFIFO 投递失败，导出层不消费快照请求；下一帧会继续尝试设置 `reserved`，避免快照请求因为拥塞被误确认。
- `nalu_ipc_submit_stream()` 对每次尝试递增 `seq`；小核收到的 `seq` 若跳号，表示大核低延迟模式下发生了主动跳帧。
- `nalu_ipc_release_callback()` 用同一条消息里的 `submit_time_ms` 计算 `read_done_age_ms`；若该值长期低于 `100ms` 且 `pending` 稳定在 `0-1`，大核 DATAFIFO/VENC 路径基本可排除。
- 大核稳定性诊断版在 `nalu_ipc_submit_stream()` 的初始化、drain、空间查询、pending、write和WRITE_DONE失败出口统一打印 `[ipc:submit-fail]`；`nalu_ipc_drain_releases()` 在返回错误或pending无进展时打印 `[ipc:drain]`。`stream_export_submit_venc_stream()` 的早退打印 `[export:fail]`，`stream_thread()` 同时记录 `status_cur_packs/output_pack_cnt/release_by_caller`。零包修复后，`stream_thread()` 每轮在查询 VENC 前主动 flush release 通知，并把成功返回的零 pack 截止在导出层之前，避免 `pending=1 -> VENC 无包 -> 无有效提交可触发 drain` 的自锁。
- `nalu_ipc_log_stats()` 低频输出 `seq/pending/seq_gap/drop_current/read_done_seq/read_done_age_ms/ok/datafifo_full/pending_full/write_fail/release_done`，用于判断积压发生在哪一段；`nalu_ipc_log_pending_slots()` 在 pending 满时输出每个未释放 slot 的年龄，重点排查 `pending_full_after_drain` 且 `avail=1536` 的场景。
- 小核RTSP源码已迁入仓库 `small_core/`；DATAFIFO ring仍为3项，小核在已有参数集且积压时READ_DONE旧项、保留最新项，不能通过增加队列深度掩盖延迟。

### AI 旁路链路（保留实现，默认禁用）

```text
ai_motion_thread()
-> ai_frame_try_get()
-> motion_adapter_process()
-> motion_detect_process()
-> 连续帧确认 / 锁存 / 冷却消抖
-> ai_frame_release()
-> osd_set_motion_visible()
-> stream_export_request_snapshot()
```

关键数据：

- 默认 `AI_BRANCH_ENABLE=1` 时整条旁路进入运行期：初始化 AI VB pool 和 VICAP 通道2，启动运动线程并继续产生独立快照请求；显式 `AI_BRANCH_ENABLE=0` 时整条旁路被预处理移除。
- AI 线程只读取 `AI_VICAP_CHN` 的低清 Y 平面，主通道编码链路不被 CPU 拷贝打断。
- `ai_gray_frame_view.y` 是 AI 低清通道 NV12 的 Y 平面，可直接作为灰度图输入；`stride` 是每行跨度，可能大于 `width`。
- `motion_detect.c` 是当前实际生效的 B 侧强符号算法；`motion_detect_default.c` 继续保留弱符号兜底示例。
- `motion_detect_process()` 只输出 `is_motion` 和 `motion_score`，不直接操作 OSD、VICAP、VENC、线程或帧释放。
- `motion_adapter_process()` 先把算法结果送入消抖状态机；连续 `3` 帧运动后才生成事件，持续运动期间锁存避免每帧触发，连续 `5` 帧静止后释放锁存，且两次事件之间至少间隔 `1000ms`。确认事件同时携带 `request_snapshot = 1` 和 `osd_duration_ms = 1000`。
- 默认 `osd_set_motion_visible()` 只提交显隐目标，`stream_thread()` 在 VENC query/get 前调用 `osd_poll_auto_hide()` 完成 buffer 更新和自动隐藏；事件日志用 `osd_enabled=1` 标识启用态。AI frame 仍先释放，快照请求随后独立进入导出层。
- 快照请求只进入 `stream_export.c` 的小型 FIFO，不调用主动 IDR API；后续码流处理等待编码器按 GOP 自然产生带 `K_VENC_HEADER` 的 stream，超过 `VENC_GOP + 2` 帧后使用现有兜底选择。大核不写 SD 卡、不复制图片、不做 JPEG 编码，只在后续 DATAFIFO 消息的 `reserved` 里设置快照 bit。

### 清理链路

```text
sig_handler()
-> g_running = 0
-> pipeline_cleanup()
-> Cleanup flow: producer-stop-before-stream-stop
-> 默认 AI_BRANCH_ENABLE=1 时 ai_motion_thread_stop() 并有限等待；显式0时跳过
-> kd_mpi_vicap_stop_stream()
-> kd_mpi_sys_unbind()
-> kd_mpi_vicap_deinit()
-> stream_thread_stop()
-> g_stream_running = 0
-> 有限等待 g_stream_exit_sem
-> stream_export_deinit()
-> kd_mpi_venc_stop_chn()
-> osd_deinit()
-> kd_mpi_venc_destroy_chn()
-> kd_mpi_venc_close_fd()
-> kd_mpi_vb_exit()
```

关键数据：

- `sig_handler()` 只修改 `sig_atomic_t` 标志，不调用 `LOG()` 或 `printf()`。
- `stream_thread()` 不直接随 `g_running` 退出；cleanup 先停止生产者 VICAP，让 stream 线程继续 drain VENC，再通过 `stream_thread_stop()` 有限等待退出，避免停止 VICAP 时下游 VENC buffer 无人消费。
- 默认 AI 清理路径调用 `ai_motion_thread_stop()`，线程停止等待仍有 `AI_THREAD_EXIT_TIMEOUT_MS` 上限；显式关闭 AI 时预处理移除该生命周期。
- 默认三个运行期 OSD API 使用启用态：AI 线程只提交目标状态，stream 线程轮询复制/清零素材并 flush cache，退出时 detach 并释放 OSD buffer；显式关闭 OSD 时才使用无副作用桩。
- DATAFIFO 模式下，`stream_export_deinit()` 会调用 `nalu_ipc_deinit()`。
- `stream_export_deinit()` 会丢弃尚未投递的快照请求；`nalu_ipc_deinit()` 会强制释放尚未被小核 `READ_DONE` 的 pending stream。
- 默认恢复版实际调用链遵守“创建后、启动前 attach；停止后、销毁前 detach”的顺序；显式 `VENC_OSD_ENABLE=0` 时调用链中不存在 attach/detach。

## 3. 数据追踪

典型输入：GC2093 输出一帧 `1920x1080` 图像；默认恢复版同时配置 `640x480` AI 旁路输出。

1. `main()` 先打印 `[diag] ai_branch=1` 和 `experiment=ai_osd_restore` 指纹，再初始化主帧、VENC stream、AI和OSD四个独立VB pool，创建VENC、完成一次OSD region配置、启动VENC、初始化主/AI VICAP通道、绑定、启动VICAP、初始化导出层并启动AI与stream线程。主动IDR仍关闭。
2. VICAP 主通道属性请求 `VICAP_OUTPUT_FPS=15`，并通过 `kd_mpi_sys_bind()` 自动送入 VENC；VENC 的 `SRC_FPS=15`、`DST_FPS=15` 与该请求节拍对齐，不再按“30fps 输入、15fps 输出”做码控。主通道实际节拍不从传感器标称帧率推测，而由 `[health:venc]` 的实测 fps 和 `pts_delta_min_us/pts_delta_max_us` 验证；15fps 正常 PTS 间隔应接近 `66667us`。
3. `stream_thread()` 每轮先调用 `osd_poll_auto_hide()` 应用 pending 的 ARGB buffer（含自动隐藏和 cache flush），再调用 `stream_export_flush()` 回收已由小核 `READ_DONE` 的 pending stream，最后查询 VENC pack 数并按量提供描述数组给 `kd_mpi_venc_get_stream()`。查询数或返回数超过 8 时，当前帧仅记录 `[venc:pack-overflow]`，完整 release 后丢弃；未超限帧才继续进入新鲜度和导出层。
4. 连续两帧漂移超过 `70ms` 时直接释放迟到 stream；漂移恢复到 `35ms` 后，`stream_thread()` 才继续把新鲜 `k_venc_stream` 提交给 `stream_export_submit_venc_stream()`。
5. 如果是 `STREAM_EXPORT_LOCAL_LOG`，pack 长度被打印到终端，导出层返回 `release_by_caller = K_TRUE`，大核立即调用 `kd_mpi_venc_release_stream()`。
6. 如果是 `STREAM_EXPORT_DATAFIFO`，导出层调用 `nalu_ipc_submit_stream(..., flags)`；函数先分配新的 `seq` 和 `submit_time_ms`，再把 pack 和 `reserved` 标志整理为 `mpp_nalu_ipc_msg` 写入 DATAFIFO，小核按 `phys_addr + len` mmap 并发送 RTSP/RTP。
7. DATAFIFO 成功接管时导出层返回 `release_by_caller = K_FALSE`，`stream_thread()` 不立即释放该 stream，pending 深度加 1。
8. 如果 DATAFIFO 或 pending 已满，本次 `seq` 被计入 drop，导出层返回错误并要求 `stream_thread()` 立即释放当前 stream。小核下一次收到更大的 `seq` 时可用跳号判断大核已丢帧。
9. 小核 `datafifo_rtp_sender_loop()` 通过 `nalu_datafifo_read()` 取出描述符，`datafifo_copy_msg_packs()` 把需要发送或保存的 H.265 pack 复制到小核自有内存，随后立即通过 `nalu_datafifo_read_done()` 调用 `DATAFIFO_CMD_READ_DONE`。大核 release callback 用同一个 `seq` 计算 `read_done_age_ms`，再重建 `k_venc_stream` 并释放 VENC buffer，pending 深度减 1。
10. READ_DONE 完成后，小核才调用 `datafifo_process_copied_frame_for_playback()`，沿 `datafifo_process_h265_buffer_for_playback() -> datafifo_process_nalu_for_playback() -> send_h265_nalu_rtp() -> send_rtp_packet()` 解析 Annex-B、执行 H.265 FU 分片并发送 RTP。UDP socket 由 `create_udp_socket_only()` 配置 `O_NONBLOCK`、`SO_SNDBUF=128KiB` 和低延迟 TOS；发送队列繁忙时丢当前包并等待下一次自然随机访问帧，不能阻塞下一轮 DATAFIFO 读取。`rtp_clock_next_timestamp()` 位于独立 `rtp_clock` 模块：首帧 PTS 为0时先按固定步长推进，下一帧首次获得有效PTS后建立PTS/RTP基准；正常前向增量不超过1秒时保持原映射；前向跳变超过1秒时按完整PTS差推进90kHz RTP时间戳并重建基线，当前帧`pts_mode=1`；PTS回退仍只推进固定step后重建基线。`datafifo_rtp_sender_loop()`检测到`rebase_count`增加后先输出`[rtp:rebase]`，再于处理当前帧前设置`wait_for_idr=1`、清零跳过计数并请求缓存参数集；当前帧若含自然IDR/CRA就立即先发VPS/SPS/PPS后恢复，否则跳过普通VCL等待下一自然随机访问帧。该恢复不主动请求IDR，不重建RTSP会话，也不重置RTP序号、SSRC或时钟状态。compact诊断以`[health:small]`每60秒关联UDP队列与READ_DONE进度；`[rtp:diag]`只在outq达到实际缓冲75%或时间异常时打印。
11. 主视频数据链路初始化后，默认 AI 线程从 15fps 低清通道 dump NV12 Y 平面并进入运动检测；显式设 `AI_BRANCH_ENABLE=0` 时才在此结束旁路。
12. `motion_detect.c` 的 B 侧强符号帧差算法首帧建立上一帧缓存，后续抽样比较 Y 分量，输出千分比 `motion_score` 和 `is_motion`；`motion_detect_default.c` 的弱符号默认算法仍保留为兜底示例。
13. `motion_adapter_process()` 对算法输出做消抖：连续 `3` 帧运动才生成事件，持续运动期间锁存，连续 `5` 帧静止后释放锁存，短时间重复运动受 `1000ms` 冷却限制。
14. 确认运动事件产生后，`ai_motion_thread()` 先释放当前 AI frame，再调用 `osd_set_motion_visible()` 提交 1000ms 显示请求，并通过 `stream_export_request_snapshot()` 登记快照请求，最后输出带 `osd_enabled=1` 的 `[event:motion]`。stream 线程随后复制 ARGB 素材并自动隐藏，快照 FIFO、自然 GOP 等待和 DATAFIFO bit0 链路继续工作。
15. `stream_export_submit_venc_stream()` 发现待快照请求后，优先等待带 `K_VENC_HEADER` 的后续 stream；等到目标 stream 或超过等待帧数后，把 `MPP_NALU_IPC_FLAG_SNAPSHOT` 写入 `reserved` 并在 DATAFIFO 投递成功后消费请求。
16. 小核 DATAFIFO/RTP 线程读取到 `reserved & MPP_NALU_IPC_FLAG_SNAPSHOT` 后，必须在 `DATAFIFO_CMD_READ_DONE` 前复制被标记帧的编码数据，再投递给小核快照队列；大核仍只等待 READ_DONE 后释放 VENC buffer。
17. 退出时，主线程不先等待 stream 线程；默认 `pipeline_cleanup()` 直接停止 VICAP 生产者，让 stream 线程继续 drain VENC；AI 启用态才先停止 AI 线程。
18. VICAP stop/unbind/deinit 后，`stream_thread_stop()` 设置 `g_stream_running = 0` 并有限等待 stream 线程退出，再释放导出层、VENC 和 VB；`osd_deinit()` 在 VENC stop 后、destroy 前 detach 2D 并释放 OSD buffer。
19. 主循环当前只等待 `SIGINT`/`SIGTERM` 让 `sig_handler()` 清零 `g_running`；原 `AUTO_EXIT_SEC=600` 自动退出验收逻辑保留在源码禁用块中，默认不会在 10 分钟后退出。

最终输出：

- 本地日志模式：终端限频打印 NALU 大小和统计信息。
- DATAFIFO 模式：终端打印 DATAFIFO 物理地址，小核通过该地址读取 NALU 描述并推流。
- 两种模式默认持续运行，收到退出信号后应打印 `Cleanup complete` 和 `Pipeline test PASSED.`。

### 码流描述的三层关系

```text
k_venc_stream / k_venc_pack
  SDK get_stream()/release_stream 使用；packs[] 是大核线程栈上的临时描述数组。

mpp_nalu_ipc_msg
  跨核 DATAFIFO 消息；保存 seq、frame_pts、submit_time_ms、total_len、reserved 以及每个 pack 的 phys_addr、len、pts、type 等小核消费、快照识别和延迟测量所需字段。

g_pending[].ipc_msg
  大核 DATAFIFO 后端的延迟释放凭据；小核 READ_DONE 后用它重建 k_venc_stream 并 release。
```

真实 H.265 数据只在 VENC buffer 中，不会复制到 DATAFIFO 或 pending 表；多份结构复制的是描述信息，用于跨越 SDK、跨核和生命周期三个边界。

## 4. 日志策略

默认运行日志只保留有状态判断价值的内容：

- 保留：`[diag] ai_branch=1`、`experiment=ai_osd_restore`、模块初始化成功/失败、传感器识别结果、VI->VENC 绑定、VICAP 启动、导出模式、DATAFIFO `phy_addr`、运行中统计、运动事件、退出清理和 PASS/FAIL。
- 保留：所有 SDK/API 失败返回码，以及 DATAFIFO full、pending full、drop current、pending 强制释放等会影响资源释放或跨核对接的异常状态。
- 正常期 `[health:venc]` 每5秒输出实测fps/kbps、帧大小、PTS间隔、QP、`max_query_packs` 和 `pack_overflow_count`，`[health:ipc]`、`[health:small]` 仍每60秒输出；大小核字段可按seq、PTS年龄、pending、read_done和outq直接关联。
- VENC 查询数或返回数超过 8 时，`[venc:pack-overflow]` 在首次及每30次输出 query/returned packs、完整字节、PTS、累计次数和最大查询数；该帧不进入 8-pack DATAFIFO。
- VENC连续500ms无有效stream时输出`[stall:venc] state=start`，之后每1秒ongoing，恢复时一条recovered；小核连续500ms无有效DATAFIFO消息采用同样节奏输出`[stall:small]`。500ms前不打印旧query/get/empty/idle轮询日志。
- 小核`[rtp:diag]`保留outq、实际`SO_SNDBUF`、RTP step和clock drift，但只在outq达到实际缓冲75%、时间戳异常或查询失败时输出；outq持续高位和重复EAGAIN每秒最多一条，首次EAGAIN、PTS rebase仍立即记录。
- RTP PTS 不连续时保留 `[rtp:rebase] count/from_pts/to_pts/rtp_ts` 和紧随其后的 `PTS rebase, waiting random access`；异常帧保持 RTP 单调，随后普通 VCL 不发送，自然 IDR/CRA 到来时日志顺序应为 `sent cached VPS/SPS/PPS -> random access start`，下一帧应恢复 `pts_mode=1`。
- 默认启动必须出现 `[diag] ai_branch=1` 和 `[diag] experiment=ai_osd_restore venc_osd=1 runtime_buffer_writes=1`、`VENC 2D OSD init OK`；运行期应出现 AI motion thread、`[event:motion]` 和按请求产生的 `[osd:buffer]`。显式 AI-off/no-OSD 对照才使用旧的禁止项。
- 大核异常保留`[ipc:submit-fail]/[ipc:drain]/[export:fail]`；小核大帧正常成功静默，只有copy/READ_DONE超过5ms、RTP超过15ms或失败才输出`[datafifo:large]`。
- 快照请求队列满仍打印；成功的请求入队、DATAFIFO投递、flag读/READ_DONE不再逐级打印。小核同步处理失败合并为首个和每10次`[snapshot:fail]`；成功只保留captured及后台实际保存路径，不改变bit0协议。
- 省略：VICAP channel format、线程 timeout/max_packs、AI 首帧指针/stride 等不会帮助区分积压边界的纯系统参数。
- 若需要排查参数配置，优先查源码宏和板端专项调试日志，不在默认稳定性运行日志里长期打印。

## 5. 回归测试

- `big_core/mpp_pipeline/tests/test_venc_pack_capacity.sh`：禁止把 `status.cur_packs` 静默截断为 8，验证超限时按查询数分配描述数组、完整 release 并释放临时内存、不进入 DATAFIFO，以及 overflow/健康日志字段完整。
- `big_core/mpp_pipeline/tests/test_ai_branch_disabled_rules.sh`：预处理 `AI_BRANCH_ENABLE=0` 的 VB、VICAP、main 和 cleanup 源码，验证显式 AI-off 覆盖、两 pool、无通道2配置、无运动线程生命周期及 `[diag] ai_branch=%u` 启动指纹。
- `big_core/mpp_pipeline/tests/test_frame_rate_config.sh`：验证 `SRC_FPS/DST_FPS/VICAP_OUTPUT_FPS` 均为15fps、`ENC_BITRATE=1500kbps`、VENC 专用5秒健康统计间隔、启动码率指纹，以及 `[health:venc]` 实测码率/帧大小/PTS/QP 字段。
- `big_core/mpp_pipeline/tests/test_datafifo_stability_diag.sh`：源码级验证大核submit/drain/export/VENC失败阶段字段齐全，并确保诊断版没有在提交失败后新增恢复flush。
- `small_core/tests/test_workspace_build_rules.sh`：验证工作区只构建 `user/rtsp_sender_withsd_compactdiag`、自动创建纳入Git的 `user/` 目录，且 `make verify` 不依赖未迁入的历史 ELF。
- `small_core/tests/test_rtp_queue_probe.sh`：源码级小核诊断回归，验证 `SIOCOUTQ`、实际 `SO_SNDBUF`、RTP/墙钟漂移字段、75% outq阈值，并锁定不修改128KiB缓冲和RTP pacing。
- `small_core/tests/test_rtp_clock.sh`：主机侧编译并执行真实 `rtp_clock` 模块，验证零 PTS 首帧、正常映射、超过1秒前向跳变按完整PTS差推进且当前/下一帧保持PTS模式，以及PTS回退仍只推进单步。
- `small_core/tests/test_rebase_idr_wait.sh`：验证 PTS 重锁后在处理当前帧前进入自然随机访问等待、清零跳过计数并请求缓存参数集；普通 VCL 不发送，IDR/CRA 到来时先发 VPS/SPS/PPS，且不主动请求 IDR、不重置 RTP 序号/SSRC/时间戳状态。
- `small_core/tests/test_datafifo_health_diag.sh`：源码级验证小核read/read_done健康字段、大帧慢/失败阶段日志及历史目标声明。
- `small_core/tests/test_compact_diag.sh`：主机状态机测试与源码规则合并验证60s/500ms/1s、自适应stall、慢路径阈值、OPTIONS静默、快照日志合并和当前构建指纹。
- 小核本轮唯一生成并纳入 Git 的目标为 `small_core/user/rtsp_sender_withsd_compactdiag`；历史 ELF 与 `girlshy.h265` 仍未迁入。
- `big_core/mpp_pipeline/tests/test_motion_detect_b.c`：主机侧编译并运行，验证 B 侧帧差算法的首帧基线、stride padding、运动分数和非法输入边界。
- `big_core/mpp_pipeline/tests/test_motion_adapter_debounce.sh`：主机侧编译并运行，验证 `motion_adapter_process()` 的连续帧确认、锁存、静止释放、冷却限制和确认事件会申请快照。
- `big_core/mpp_pipeline/tests/test_osd_buffer_rules.sh`：源码级验证条件编译中保留的启用态实现仍具有固定 VENC OSD region、MMZ 素材复制/清零/cache flush、stream 线程调用顺序和 `[osd:buffer]` 字段，禁止运行期重复调用 VENC 2D 参数接口。
- `big_core/mpp_pipeline/tests/test_noosd_ab_rules.sh`：验证显式 no-OSD 覆盖、实验指纹和运动事件字段完整，禁用分支只有无副作用桩且快照请求不依赖OSD开关。
- `big_core/mpp_pipeline/tests/test_ai_osd_enabled_rules.sh`：验证默认 `AI_BRANCH_ENABLE=1`、`VENC_OSD_ENABLE=1`、AI pool/通道2/运动线程生命周期和启用态硬件 OSD 实现。
- `big_core/mpp_pipeline/tests/test_variant_build_rules.sh`：验证 SCons 接受 `AI_BRANCH_ENABLE`/`VENC_OSD_ENABLE` 参数并将其作为预处理宏传递，保证 A/B ELF 不依赖未生效的 `CCFLAGS` 环境变量。
- `big_core/mpp_pipeline/tests/test_osd_vb_pool_rules.sh`：预处理四种AI/OSD组合，验证pool总数分别为2/3/3/4、OSD独立pool索引与192KiB单块配置，并要求OSD初始化记录实际pool ID。
- `big_core/mpp_pipeline/tests/test_stream_freshness.sh`：主机侧编译并运行纯状态机测试，验证零 PTS Header、首帧基线、正常节奏、连续两帧确认、`70/35ms` 追帧滞回、最大漂移、丢帧计数和 PTS 回退重建基线。
- `big_core/mpp_pipeline/tests/test_frame_rate_config.sh`：源码级配置回归，验证 VENC `30→15fps`、VICAP 帧率请求、主动 IDR 默认关闭和自然 `GOP=8`。
- `big_core/mpp_pipeline/tests/test_shutdown_source_rules.sh`：源码级线程与退出路径回归，验证保留的启用态仍遵守 AI 线程只提交请求、stream 线程在 VENC query/get 前处理 buffer、请求函数不调用 VENC 驱动，以及信号处理、有限等待和清理顺序正确。
- `big_core/mpp_pipeline/tests/test_low_latency_datafifo_rules.sh`：源码级低延迟和快照协议回归，验证顶层/模块两级 SConscript 白名单、`OUTPUT_BUF_CNT=3`、pending 深度 `2`、FIFO entries `3`、有界 drain、取流循环先flush再查询VENC、无stream原因进入自适应stall诊断且零pack在导出前被拦截、源帧新鲜度、快照标志和导出层初始化顺序。
- 大核完整验证仍以 `RTT_EXEC_PATH="/home/ubuntu/k230_sdk/toolchain/riscv64-linux-musleabi_for_x86_64-pc-linux-gnu/bin" scons -j4` 为准，板端运行 `big_app.elf` 后先确认 `[diag] ai_branch=1`、`experiment=ai_osd_restore`、`VENC 2D OSD init OK` 和 `AI motion thread create OK`，再检查 VB/VENC/VICAP 主链路初始化、`VI->VENC bind OK` 和稳定码流；运动事件应带 `osd_enabled=1`。默认版本运行600秒并反复制造大面积运动时不得出现超过500ms的 `[stall:venc]`，阶段二还需完成至少20次事件与10分钟运行，手动退出后检查 `Pipeline test PASSED.`。
- AI-on/OSD-off 对照构建使用 `scons -c && scons -j4 VENC_OSD_ENABLE=0`；启动必须出现 `experiment=noosd_ab`、`ai_branch=1` 和 `AI motion thread create OK`，不得出现 `VENC 2D OSD init OK`。每次切换 ELF 后必须以该次启动日志重新获取 DATAFIFO `phy_addr`。
